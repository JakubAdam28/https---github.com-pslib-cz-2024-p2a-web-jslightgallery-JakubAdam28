export const TIMINGFUNC_MAP = {
    linear: (t) => t,
    'ease': (t) => t * t / (2 * (t * t - t) + 1),
    'ease-in': (t) => t * t,
    'ease-out': (t) => t * (2 - t),
    'ease-in-out': (t) => (t < 0.5 ? 2 * t * t : -1 + (4 - 2 * t) * t),
};
